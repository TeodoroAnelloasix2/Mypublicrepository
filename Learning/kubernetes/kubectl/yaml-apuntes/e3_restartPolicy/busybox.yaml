apiVersion: "v1"
kind: Pod
metadata:
  name: busyonfailure
  labels:
    app: appbusy
spec:
  containers:
  - name: busy-ctn
    image: busybox
    command: [ "sh", "-c", "echo Simulando fallo && sleep 10 && exit 1" ]
    restartPolicy: OnFailure
