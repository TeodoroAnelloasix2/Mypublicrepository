apiVersion: v1 
kind: Service
metadata:
  name: deploy-nodeport
spec:
  type: NodePort
  selector:
    app: simple-nginx-app
    env: prod
  ports:
    - name: http
      port: 80
      targetPort: 80